import React from "react";
import Question from "../Components/Question";

const Quiz = ({ questions }) => {
  return (
    <div>
      <Question />
    </div>
  );
};

export default Quiz;
